# PacMan

![PacMan](docs/pacman.png)

This is a rust implmentation of [pacmancode.com](https://pacmancode.com), rendered with Kitty graphics.

Run targets:

- `cargo run`

Run this inside `kitty`, `ghostty`, `warp` or another terminal that supports the
Kitty graphics protocol.
